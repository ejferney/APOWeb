# Frontend environment variables (copy to .env for local overrides)
# Base URL of the API server. Leave unset for local dev (defaults to http://localhost:5000).
# For production set this to your deployed server, e.g. https://apoweb-api.onrender.com
VITE_API_URL=http://localhost:5000
